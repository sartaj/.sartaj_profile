# Defaults
alias ls="ls -a"

# Services
alias serve="browser-sync start --server --files ./*.* --port"

# Git Aliases
alias gitclean="git pull; git fetch -p; git branch --merged | grep -v "\*" | xargs -n 1 git branch -d"
